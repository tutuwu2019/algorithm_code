# 应用


## 区块链中的应用

> 应用场景：交易哈希、区块哈希、默克尔树根、工作量证明、地址生成


## 数字签名中的应用

> 应用场景：数字签名


```text
1. 发送方：
   - 用SHA-256计算消息的哈希值
   - 用私钥对哈希值进行加密 → 得到数字签名
   - 发送（消息 + 签名）

2. 接收方：
   - 用SHA-256重新计算消息哈希值
   - 用发送方的公钥解密签名 → 得到原始哈希值
   - 比较两个哈希值是否一致

```
